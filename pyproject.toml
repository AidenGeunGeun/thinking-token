[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "thinking-tokens"
version = "0.1.0"
description = "tau2-bench experiments for thinking token retention"
requires-python = ">=3.12,<3.14"
dependencies = []

[tool.setuptools]
packages = ["src"]
